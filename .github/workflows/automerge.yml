name: automerge
on:
  check_run:
    types:
      - completed
  pull_request:
    types:
      - labeled
      - unlabeled
      - opened
      - edited
      - ready_for_review
      - reopened
      - unlocked
      - push
  pull_request_review:
    types:
      - submitted
  status: {}
jobs:
  automerge:
    name: Auto Merge
    runs-on: ubuntu-latest
    steps:
      - name: Merge pull requests
        uses: blake-newman/automerge-action@288ffc4
        env:
          GITHUB_TOKEN: "${{ secrets.GH_TOKEN }}"
          LABELS: "!wip, required"
